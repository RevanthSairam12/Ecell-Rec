rules_version = '2';

// Firebase Storage Security Rules
// Storage is no longer used - all uploads are blocked
service firebase.storage {
  match /b/{bucket}/o {
    
    // Deny all storage access
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
